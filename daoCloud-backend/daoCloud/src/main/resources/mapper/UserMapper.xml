<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fzu.gcxl.daocloud.mapper.UserMapper">
    <insert id="insertUser" parameterType="User">
        insert into user (user_name, user_school, user_department, user_no, user_phone, user_role)
        values (#{username},#{userSchool},#{userDepartment},#{userNo},#{userPhone},#{userRole})
    </insert>
    <select id="queryUserByName" resultType="user">
        select * from user where user_name = #{userName};
    </select>


</mapper>